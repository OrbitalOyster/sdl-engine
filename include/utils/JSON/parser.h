#ifndef PARSER_H
#define PARSER_H

#include "JSON.h"

//struct Token *parse_next_token(struct JSON *json,
//                               int (*get_next_char)(struct JSON *, int));

#endif /* PARSER_H */
